package week05;

public class ReplacePractice {
    public static void main(String[] args) {
       /* String word ="github";
        System.out.println(word.replace("hub","lab"));// an object is created but not assigned
        // to a reference (so we an unreferenced object) garbage collector cleans that object
        // JAVA memory usage topic

        System.out.println("word = " + word);
        System.out.println(word.replace('i','o'));
        String searchResult="1-48 of over 6,000 results for java";
        String actualResult = searchResult.replace("1-48 of over","");
        System.out.println("result = " + result);
        String expectedResult="6000";

        if(expectedResult.equals(actualResult)){
            System.out.println("PASS");
        }else{
            System.out.println("FAIL");
        }*/
    }
}
